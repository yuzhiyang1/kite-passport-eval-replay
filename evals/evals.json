[
  {
    "id": 1,
    "prompt": "Show me my recent activity",
    "expected_output": "Agent runs kpass activity --output json and renders each event as a display card with kind, status, timestamp, and relevant details",
    "assertions": [
      "kpass activity",
      "--output json",
      "━━━"
    ]
  },
  {
    "id": 2,
    "prompt": "Show me only my wallet transfers",
    "expected_output": "Agent runs kpass activity --kind wallet_transfer --output json and renders wallet transfer events as display cards with amount, asset, and tx hash",
    "assertions": [
      "kpass activity",
      "--kind wallet_transfer",
      "--output json"
    ]
  },
  {
    "id": 3,
    "prompt": "I can see 10 events on the first page of my activity — show me the next page of older activity",
    "expected_output": "Agent runs kpass activity --limit N --offset N --output json to paginate results beyond the first page, using an offset equal to the number of events already shown",
    "assertions": [
      "kpass activity",
      "--limit",
      "--offset",
      "--output json"
    ]
  },
  {
    "id": 4,
    "prompt": "Log me in to Kite Passport",
    "expected_output": "Agent runs setup.sh, then kpass login init --email <EMAIL> --client agent --output json --no-interactive, asks the user for the OTP code, then runs kpass login verify with KPASS_LOGIN_CODE",
    "assertions": [
      "kpass login init",
      "--client agent",
      "--output json",
      "--no-interactive"
    ]
  },
  {
    "id": 5,
    "prompt": "I need to create a Kite Passport account",
    "expected_output": "Agent runs setup.sh, then kpass signup init --email <EMAIL> --client agent --output json --no-interactive, asks the user for the 8-character sign-up code from their email, then runs signup exchange with KPASS_SIGNUP_CODE (signup poll is optional and not part of the primary flow)",
    "assertions": [
      "kpass signup init",
      "--client agent",
      "--output json",
      "--no-interactive"
    ]
  },
  {
    "id": 6,
    "prompt": "Am I logged in?",
    "expected_output": "Agent runs kpass me --output json and reports the current user email and user_id if authenticated, or tells the user they are not logged in",
    "assertions": [
      "kpass me",
      "--output json"
    ]
  },
  {
    "id": 7,
    "prompt": "Create an agent session to access api.example.com",
    "expected_output": "Agent preflights api.example.com to discover 402 payment requirements, then constructs a delegation JSON with task.summary, payment_policy (max_amount_per_tx and max_total_amount), and optional execution_constraints, then passes it to kpass session create --delegation '<INNER-JSON>' --output json",
    "assertions": [
      "delegation",
      "payment_policy",
      "kpass session create",
      "--output json"
    ]
  },
  {
    "id": 8,
    "prompt": "Set up a session with a $5 budget to call the Exa search API at exa.mpp.tempo.xyz",
    "expected_output": "Agent preflights exa.mpp.tempo.xyz to discover payment requirements, then constructs a delegation with max_total_amount reflecting the $5 total budget and max_amount_per_tx set to the per-request price, then calls kpass session create",
    "assertions": [
      "delegation",
      "max_total_amount",
      "kpass session create"
    ]
  },
  {
    "id": 9,
    "prompt": "Find paid APIs for weather data",
    "expected_output": "Agent runs setup-ksearch.sh, then ksearch service list --query weather --output json, and renders each service as a display card with name, summary, pricing, and base URL",
    "assertions": [
      "ksearch service list",
      "--query",
      "--output json"
    ]
  },
  {
    "id": 10,
    "prompt": "Show me details about the Exa service",
    "expected_output": "Agent runs ksearch service get <exa-service-id> --output json (service id passed as the positional argument, not a --service-id flag) and renders the service detail card with all endpoints, pricing, and payment approach",
    "assertions": [
      "ksearch service get",
      "--output json"
    ]
  },
  {
    "id": 11,
    "prompt": "What paid services are available on Kite?",
    "expected_output": "Agent runs ksearch service list --output json and displays all available services with name, summary, starting price, and tags",
    "assertions": [
      "ksearch service list",
      "--output json",
      "━━━"
    ]
  },
  {
    "id": 12,
    "prompt": "What agents are registered on my account?",
    "expected_output": "Agent runs kpass user agents --output json and displays a list of registered agents with their IDs, types, and registration dates",
    "assertions": [
      "kpass user agents",
      "--output json"
    ]
  },
  {
    "id": 13,
    "prompt": "Show me my active spending sessions",
    "expected_output": "Agent runs kpass user sessions --status active --output json and displays each active session with its status, delegation details, and spending limits",
    "assertions": [
      "kpass user sessions",
      "--status active",
      "--output json"
    ]
  },
  {
    "id": 14,
    "prompt": "Set up a spending session so I can make payments",
    "expected_output": "Agent runs kpass agent:register --type claude --output json to register the agent, then constructs a delegation and runs kpass session create, then opens the approval URL for the user",
    "assertions": [
      "kpass agent:register",
      "session create",
      "--output json"
    ]
  },
  {
    "id": 15,
    "prompt": "Use my existing session to make a payment",
    "expected_output": "Agent calls kpass session create --delegation '<INNER-JSON>' --output json for the payment; reuse is detected automatically at create time -- the CLI mechanically checks existing active sessions (asset, per-tx, budget, TTL, scope) and returns a reuse_available result with candidates if one already covers the request. The agent does not need to run kpass session list first -- that command is optional, useful only for diagnostics or manual inspection. On reuse_available, the agent confirms the goal/merchant genuinely matches, then runs the returned next_command (session use --session-id <id>) to reuse it before proceeding with payment execution.",
    "assertions": [
      "session create",
      "reuse_available",
      "--output json"
    ]
  },
  {
    "id": 16,
    "prompt": "Check the status of my pending session",
    "expected_output": "Agent runs kpass session status --request-id <request_id> --output json (add --wait to poll) to check whether the pending approval request is still pending, was approved, rejected, or expired. Note: kpass session list only returns actual sessions with status active or expired -- it does not surface a request still awaiting approval.",
    "assertions": [
      "kpass session status --request-id",
      "--output json"
    ]
  },
  {
    "id": 17,
    "prompt": "Find me a phone charger under $10",
    "expected_output": "Agent runs kpass shop:search \"phone charger\" --output json with max-results, displays product cards with title, price, and provider, then asks which to add to cart",
    "assertions": [
      "kpass shop:search",
      "--output json",
      "━━━"
    ]
  },
  {
    "id": 18,
    "prompt": "What's in my shopping cart?",
    "expected_output": "Agent runs kpass shop:cart --output json and renders each cart item as a display card with title, quantity, price, and provider, then shows the cart total",
    "assertions": [
      "kpass shop:cart",
      "--output json"
    ]
  },
  {
    "id": 19,
    "prompt": "I want to checkout and buy the items in my cart",
    "expected_output": "Agent verifies shipping info with kpass shop:shipping --output json, confirms the cart total with the user, creates a spending session with a budget matching the total, then runs kpass shop:checkout --output json only after explicit user confirmation",
    "assertions": [
      "kpass shop:checkout",
      "--output json"
    ]
  },
  {
    "id": 20,
    "prompt": "What's my wallet balance?",
    "expected_output": "Agent runs kpass wallet balance --output json and displays a card showing wallet address, chain, and each asset symbol with its balance",
    "assertions": [
      "kpass wallet balance",
      "--output json",
      "━━━"
    ]
  },
  {
    "id": 21,
    "prompt": "Send 5 USDC to 0xabc123def456",
    "expected_output": "Agent runs kpass wallet send --to 0xabc123def456 --asset USDC --amount 5 --output json and displays a confirmation card with the tx hash",
    "assertions": [
      "kpass wallet send",
      "--to 0xabc123def456",
      "--asset USDC",
      "--amount 5",
      "--output json"
    ]
  },
  {
    "id": 22,
    "prompt": "Make a paid API call to api.example.com/data",
    "expected_output": "Agent runs kpass session execute --url api.example.com/data --output json and displays the response content from the paid API endpoint",
    "assertions": [
      "session execute",
      "--url",
      "--output json"
    ]
  },
  {
    "id": 23,
    "prompt": "POST to api.example.com/submit with body {\"key\": \"value\"}",
    "expected_output": "Agent runs kpass session execute --url api.example.com/submit --method POST --body '{\"key\": \"value\"}' --output json",
    "assertions": [
      "session execute",
      "--body",
      "--output json"
    ]
  },
  {
    "id": 24,
    "prompt": "Log me out of Kite Passport",
    "expected_output": "Agent runs kpass logout --output json and confirms the user has been signed out",
    "assertions": [
      "kpass logout",
      "--output json"
    ]
  },
  {
    "id": 25,
    "prompt": "I tried to log in but got an error saying my email isn't registered — what should I do?",
    "expected_output": "Agent recognises exit code 4 / 'email not registered' and falls back to kpass signup init --email <EMAIL> --client agent --no-interactive --output json, then asks the user for the 8-character sign-up code and completes the flow with signup exchange (signup poll is optional, not required)",
    "assertions": [
      "kpass signup init",
      "--client agent",
      "--no-interactive",
      "--output json"
    ]
  },
  {
    "id": 26,
    "prompt": "The OTP code I entered didn't work — it says invalid code",
    "expected_output": "Agent recognises the invalid OTP error (exit code 3 from login verify), asks the user to re-check their email for the correct 8-character code, and retries kpass login verify with the same login_id",
    "assertions": [
      "kpass login verify",
      "login_id"
    ]
  },
  {
    "id": 27,
    "prompt": "Drop some testnet USDC into my wallet",
    "expected_output": "Agent runs kpass faucet drop --recipient <ADDRESS> --token USDC --output json and displays a Tokens Received card with the drop details",
    "assertions": [
      "kpass faucet drop",
      "--recipient",
      "--token USDC",
      "--output json"
    ]
  },
  {
    "id": 28,
    "prompt": "Send 0.5 ETH to 0xdeadbeef1234567890abcdef",
    "expected_output": "Agent runs kpass wallet send --to 0xdeadbeef1234567890abcdef --asset ETH --amount 0.5 --output json and shows the Transfer Complete card",
    "assertions": [
      "kpass wallet send",
      "--asset ETH",
      "--amount 0.5",
      "--output json"
    ]
  },
  {
    "id": 29,
    "prompt": "Show me only my Claude agents",
    "expected_output": "Agent runs kpass user agents --agent-type claude --output json and lists only agents of type claude",
    "assertions": [
      "kpass user agents",
      "--agent-type claude",
      "--output json"
    ]
  },
  {
    "id": 30,
    "prompt": "List only my active sessions",
    "expected_output": "Agent runs kpass user sessions --status active --output json and shows only sessions with active status, including remaining budget from usage fields",
    "assertions": [
      "kpass user sessions",
      "--status active",
      "--output json"
    ]
  },
  {
    "id": 31,
    "prompt": "I approved the session in my Passport app — is it active yet?",
    "expected_output": "Agent runs kpass session status --request-id <REQUEST_ID> --output json -- a single check without --wait, since the user has already signaled approval -- and reports whether the session is now active",
    "assertions": [
      "kpass session status --request-id",
      "--output json"
    ]
  },
  {
    "id": 32,
    "prompt": "Register my agent before setting up a session",
    "expected_output": "Agent runs kpass agent:register --type claude --output json and displays the registration confirmation with the new agent_id",
    "assertions": [
      "kpass agent:register",
      "--type claude",
      "--output json"
    ]
  },
  {
    "id": 33,
    "prompt": "Add a wireless mouse to my cart",
    "expected_output": "Agent runs kpass shop:search \"wireless mouse\" to find a product, then runs kpass shop:cart add --provider <PROVIDER> --external-id <ID> --quantity 1 --output json and shows the Added to Cart confirmation card",
    "assertions": [
      "kpass shop:cart add",
      "--provider",
      "--external-id",
      "--output json"
    ]
  },
  {
    "id": 34,
    "prompt": "Remove the keyboard from my cart",
    "expected_output": "Agent runs kpass shop:cart view to identify the keyboard item, then runs kpass shop:cart remove --provider <PROVIDER> --external-id <ID> --output json",
    "assertions": [
      "kpass shop:cart remove",
      "--provider",
      "--external-id",
      "--output json"
    ]
  },
  {
    "id": 35,
    "prompt": "Empty my shopping cart",
    "expected_output": "Agent runs kpass shop:cart clear --output json and confirms the cart has been cleared",
    "assertions": [
      "kpass shop:cart clear",
      "--output json"
    ]
  },
  {
    "id": 36,
    "prompt": "Update my shipping address to 123 Main St, Springfield, IL 62701",
    "expected_output": "Agent runs kpass shop:shipping update with --name, --email, --line1, --city, --state, --postal flags populated and --output json",
    "assertions": [
      "kpass shop:shipping update",
      "--line1",
      "--city",
      "--postal",
      "--output json"
    ]
  },
  {
    "id": 37,
    "prompt": "What's the status of my order ORD-abc123?",
    "expected_output": "Agent runs kpass shop:order status --order-id ORD-abc123 --output json and reports the current order status and tracking info",
    "assertions": [
      "kpass shop:order status",
      "--order-id",
      "--output json"
    ]
  },
  {
    "id": 38,
    "prompt": "Find AI APIs on Kite — I only want ones that accept USDC",
    "expected_output": "Agent runs ksearch service list --query AI --asset USDC --output json (or equivalent filter combination) to surface services accepting USDC payment",
    "assertions": [
      "ksearch service list",
      "--asset USDC",
      "--output json"
    ]
  },
  {
    "id": 39,
    "prompt": "Show me more Kite services — there must be more than the first page",
    "expected_output": "Agent runs ksearch service list --limit N --cursor <CURSOR> --output json using the next_cursor value from the previous response to fetch the next page",
    "assertions": [
      "ksearch service list",
      "--cursor",
      "--output json"
    ]
  },
  {
    "id": 40,
    "prompt": "Is the Kite service catalog working right now?",
    "expected_output": "Agent runs ksearch service health --output json and reports whether the catalog backend is reachable and healthy",
    "assertions": [
      "ksearch service health",
      "--output json"
    ]
  },
  {
    "id": 41,
    "prompt": "PUT to api.example.com/resource/42 with body {\"status\": \"done\"}",
    "expected_output": "Agent runs kpass session execute --url api.example.com/resource/42 --method PUT --body '{\"status\": \"done\"}' --output json",
    "assertions": [
      "session execute",
      "--method PUT",
      "--body",
      "--output json"
    ]
  },
  {
    "id": 42,
    "prompt": "Call api.example.com/ingest with a custom header X-Api-Version: 2",
    "expected_output": "Agent runs kpass session execute --url api.example.com/ingest --headers '{\"X-Api-Version\": \"2\"}' --output json with the header passed as a JSON object",
    "assertions": [
      "session execute",
      "--headers",
      "X-Api-Version",
      "--output json"
    ]
  },
  {
    "id": 43,
    "prompt": "The paid API returned a 404 — does that mean my payment failed?",
    "expected_output": "Agent explains that a non-2xx status in x402.status_code is a response from the target service, not a payment failure — the CLI exits 0 and payment was processed. It shows the x402.status_code field and advises checking the target API's documentation.",
    "assertions": [
      "x402.status_code",
      "404"
    ]
  },
  {
    "id": 44,
    "prompt": "How much of my session budget have I used so far?",
    "expected_output": "Agent runs kpass session list --status active --output json and reads usage.spent_total vs payment_policy.max_total_amount to show remaining budget",
    "assertions": [
      "kpass session list --status active",
      "spent_total",
      "max_total_amount",
      "--output json"
    ]
  },
  {
    "id": 45,
    "prompt": "Set up a session for api.payments.xyz — I don't know what it costs yet",
    "expected_output": "Agent performs a preflight curl to api.payments.xyz to discover 402 payment requirements, displays the Payment Requirements Discovered card, then constructs a delegation and shows the Proposed Session Parameters card before asking for confirmation",
    "assertions": [
      "curl",
      "api.payments.xyz",
      "payment_policy",
      "max_amount_per_tx"
    ]
  },
  {
    "id": 46,
    "prompt": "The session approval request I sent expired before I approved it",
    "expected_output": "Agent recognises the expired session request error (exit code 3, session request expired) and creates a new session with kpass session create, presenting a new approval URL",
    "assertions": [
      "session create",
      "--output json"
    ]
  },
  {
    "id": 47,
    "prompt": "Show me my x402 API payment history",
    "expected_output": "Agent runs kpass activity --kind x402_payment --output json and renders each API payment event as a display card showing the endpoint, amount, and timestamp",
    "assertions": [
      "kpass activity",
      "--kind x402_payment",
      "--output json"
    ]
  },
  {
    "id": 48,
    "prompt": "Show me my shopping checkouts from last month",
    "expected_output": "Agent runs kpass activity --kind shopping_checkout --output json with offset pagination to reach older events, then filters by occurred_at to surface events within the requested time window",
    "assertions": [
      "kpass activity",
      "--kind shopping_checkout",
      "--output json",
      "--offset"
    ]
  },
  {
    "id": 49,
    "prompt": "Search for AI services in the Kite catalog and show me the details for the first result",
    "expected_output": "Agent runs ksearch service list --query AI --output json to get results, extracts the first service_id, then runs ksearch service get <ID> --output json (ID passed positionally, not via --service-id) to show full details",
    "assertions": [
      "ksearch service list",
      "--query",
      "ksearch service get",
      "service_id",
      "--output json"
    ]
  },
  {
    "id": 50,
    "prompt": "I want to buy a USB hub and a webcam in one order",
    "expected_output": "Agent searches for each product, runs kpass shop:cart add for each item, shows the full cart, verifies shipping, creates a single spending session sized to the combined total, and only runs kpass shop:checkout --confirmed after explicit user approval",
    "assertions": [
      "kpass shop:cart add",
      "shop:checkout",
      "--confirmed",
      "--output json"
    ]
  },
  {
    "id": 51,
    "prompt": "Set up a session for fal.ai with $0.03 per-tx and $3 total budget",
    "expected_output": "Agent preflights fal.ai to discover payment requirements, then calls kpass session create --delegation '<INNER-JSON>' --output json. The inner JSON must contain payment_policy.max_amount_per_tx (\"0.03\") and payment_policy.max_total_amount (\"3\") -- sessions are protocol-agnostic, so the delegation must NOT include payment_policy.allowed_payment_approaches or any protocol/approach field; that is detected automatically at execute time. Amount strings must be clean decimals (e.g. 0.03, not 0.029999999999999998) so the USDC 6-decimal check does not reject the value at execute time.",
    "assertions": [
      "kpass session create",
      "--delegation",
      "max_amount_per_tx",
      "max_total_amount",
      "--output json"
    ]
  },
  {
    "id": 52,
    "prompt": "Create a session for a service that uses tempo payment protocol",
    "expected_output": "Agent preflights the merchant to discover payment requirements, then constructs a delegation containing only payment_policy.max_amount_per_tx, payment_policy.max_total_amount (and optional ttl_seconds / execution_constraints), and calls kpass session create --delegation '<INNER-JSON>' --output json. Sessions are protocol-agnostic: the delegation must NOT include a payment_policy.allowed_payment_approaches field or any protocol/approach field -- legacy and silently ignored by the backend. Tempo settlement is detected automatically at execute time from the merchant's preflight response, not declared by the agent.",
    "assertions": [
      "kpass session create",
      "--delegation",
      "max_amount_per_tx",
      "max_total_amount",
      "--output json"
    ]
  },
  {
    "id": 53,
    "prompt": "I created a session in the Passport dashboard, here's the ID: session_dash7788. Use it so you can make payments.",
    "expected_output": "Agent registers itself if needed, then runs kpass session attach --session-id session_dash7788 --output json directly — no merchant preflight, no delegation JSON construction, since the owner already defined the session's policy. Displays the Attach Approval Required card with the approval_url, opens it, and polls kpass session status --request-id <id> --wait --output json until the owner approves, then shows the Session Attached card with the task summary and budget read from the approved session's delegation.",
    "assertions": [
      "kpass session attach",
      "--session-id session_dash7788",
      "--output json",
      "human_action_required",
      "kpass session status --request-id",
      "--wait",
      "Session Attached"
    ]
  },
  {
    "id": 54,
    "prompt": "Attach session_dedicated4421 to this agent — I already have it.",
    "expected_output": "Agent runs kpass session attach --session-id session_dedicated4421 --output json, which returns exit code 2 with error_code: session_not_attachable because the session is a dedicated (create-time-bound) session, not an attachable one. Agent explains the distinction and points to request-session for creating a new session, rather than retrying attach on the same ID.",
    "assertions": [
      "kpass session attach",
      "session_not_attachable",
      "request-session"
    ]
  },
  {
    "id": 55,
    "prompt": "Attach session_foreignowner902 to this agent.",
    "expected_output": "Agent runs kpass session attach --session-id session_foreignowner902 --output json, which returns exit code 6 with error_code: agent_not_same_owner because the session belongs to a different account's agent. Agent explains the ownership boundary and does not retry with a different fabricated --agent-id or suggest re-registering as a workaround.",
    "assertions": [
      "kpass session attach",
      "agent_not_same_owner",
      "--output json"
    ]
  },
  {
    "id": 56,
    "prompt": "What's my Base wallet address? I want to fund it.",
    "expected_output": "Agent runs kpass wallet address --chain base --output json and, before displaying the address, says that gas is sponsored, only USDC should be sent on Base, and ETH must not be sent.",
    "assertions": [
      "kpass wallet address",
      "--chain base",
      "--output json",
      "Gas is sponsored",
      "USDC only",
      "do not send ETH"
    ]
  },
  {
    "id": 57,
    "prompt": "Set yourself up as a buyer agent on Kite Passport so you can start finding sellers.",
    "expected_output": "Agent runs the buyer-agent-setup skill's setup.sh, then kpass agent init --output json to generate a runtime key, and reports the address and thumbprint from the result.",
    "assertions": [
      "kpass agent init",
      "--output json",
      "thumbprint"
    ]
  },
  {
    "id": 58,
    "prompt": "I created a buyer agent record, did:kite:acme-buyer-01 — bind your runtime key to it.",
    "expected_output": "Agent runs kpass agent token create --agent did:kite:acme-buyer-01 --output json to mint a bind token, then consumes it with kpass agent bind --agent did:kite:acme-buyer-01 --token <art_...> --output json. This is the default path and typically lands binding: active / status: success immediately, with no --wait and no approval_url needed. Only if token minting were to require step-up would the agent fall back to kpass agent bind --agent did:kite:acme-buyer-01 --wait --output json, surfacing the approval_url and explaining the owner must approve with their passkey -- never attempting to approve it itself.",
    "assertions": [
      "kpass agent token create --agent did:kite:acme-buyer-01",
      "kpass agent bind --agent did:kite:acme-buyer-01 --token",
      "active"
    ]
  },
  {
    "id": 59,
    "prompt": "Is your buyer identity ready to use yet?",
    "expected_output": "Agent runs kpass agent status --output json and reports readiness from binding.status: active means done and ready to hand off to buyer-find-seller, pending means still awaiting the owner's approval.",
    "assertions": [
      "kpass agent status",
      "--output json",
      "binding.status"
    ]
  },
  {
    "id": 60,
    "prompt": "kpass agent bind just failed with runtime_agent_mismatch — what now?",
    "expected_output": "Agent explains the owner pointed this runtime at a different agent record than expected, and asks the owner to confirm the correct DID or agt_ id rather than re-initializing the key or retrying bind with a guess.",
    "assertions": [
      "runtime_agent_mismatch",
      "different agent",
      "ask rather than"
    ]
  },
  {
    "id": 61,
    "prompt": "Set yourself up as a seller agent on Kite Passport.",
    "expected_output": "Agent runs setup-kagent.sh then setup.sh, confirms with kagent --version, then runs kagent init --output json to create the runtime key, reporting the address and thumbprint.",
    "assertions": [
      "kagent init",
      "--output json",
      "thumbprint"
    ]
  },
  {
    "id": 62,
    "prompt": "The owner created a seller agent record, did:kite:acme-seller-01 — bind to it.",
    "expected_output": "Agent runs kpass agent token create --agent did:kite:acme-seller-01 --output json to mint a one-time bind token (needs only the owner's plain JWT, no passkey step-up), then immediately consumes it with kagent bind --agent did:kite:acme-seller-01 --token <art_...> --output json. This default mint-then-bind path typically lands the binding active immediately -- no --wait, no approval_url to surface. Only if token minting ever came back requiring step-up would the agent fall back to the direct path (kagent bind --agent did:kite:acme-seller-01 --wait --output json), surfacing the approval_url or navigation path plus thumbprint, and explaining that only the owner can approve with a passkey.",
    "assertions": [
      "kpass agent token create --agent did:kite:acme-seller-01",
      "kagent bind --agent did:kite:acme-seller-01 --token",
      "active"
    ]
  },
  {
    "id": 63,
    "prompt": "Publish your seller card so buyers can find you.",
    "expected_output": "Agent first pins the coordination persona card with kagent card fetch --pin --output json, then publishes the seller's own card with kagent card publish --file ./card.json --output json, and checks card_hash_verified in the result rather than trusting the success status alone.",
    "assertions": [
      "kagent card fetch --pin",
      "kagent card publish --file",
      "card_hash_verified"
    ]
  },
  {
    "id": 64,
    "prompt": "Publish your storefront, rate card, and workflow terms so buyers can see what you sell.",
    "expected_output": "Agent generates skeleton files with kagent registration template --output-dir ./registration --output json, has the owner fill them in, pre-flights locally with kagent registration validate, then publishes atomically with kagent registration publish, passing all three of --storefront, --rate-card, and --workflow-terms together.",
    "assertions": [
      "registration template",
      "registration validate",
      "registration publish",
      "--storefront",
      "--rate-card",
      "--workflow-terms"
    ]
  },
  {
    "id": 65,
    "prompt": "A buyer says every proposal they send you gets refused — what's wrong?",
    "expected_output": "Agent recognizes this is very likely acceptance_policy_violation because the owner has never set an acceptance policy on this agent, and tells the owner to set one via the Passport web app's Governance page rather than treating it as a bug in the agent — it cannot read or fix its own policy.",
    "assertions": [
      "acceptance_policy_violation",
      "set its acceptance policy",
      "owner action"
    ]
  },
  {
    "id": 66,
    "prompt": "card_hash_verified came back false after I published my card — is that a problem?",
    "expected_output": "Agent explains the publish still succeeded (status success, exit 0), but the served card does not hash to what the platform reported, which will fail a buyer's own hash verification and make the listing effectively unusable until it agrees — so it should be reported to the owner rather than ignored just because the exit code was 0.",
    "assertions": [
      "card_hash_verified",
      "PUBLISHED, BUT THE HASH COULD NOT BE CONFIRMED",
      "buyers verify this hash"
    ]
  },
  {
    "id": 67,
    "prompt": "Find me a seller who can transcribe audio recordings.",
    "expected_output": "Agent starts with the natural-language first choice — ksearch find \"transcribe audio recordings\" --output json (the credential-less discovery binary, not kpass) — reads both the agents and offerings groups plus rewrite_applied, and reports matching sellers with each one's verified_tier.",
    "assertions": [
      "ksearch find",
      "--output json",
      "rewrite_applied",
      "verified_tier"
    ]
  },
  {
    "id": 68,
    "prompt": "Find a seller that can produce a dataset for under $10, ready to transact right now.",
    "expected_output": "Agent recognizes this needs a capability/price search rather than a name search, and runs ksearch agent offerings --offering-kind dataset --max-total-price-minor 10000000 --ready --output json, reading each hit's registrationHash and offering.offeringId as the registrationBasis for a later proposal.",
    "assertions": [
      "ksearch agent offerings",
      "--offering-kind dataset",
      "--max-total-price-minor",
      "--ready"
    ]
  },
  {
    "id": 69,
    "prompt": "Before I propose to did:kite:example-seller, check that its card actually verifies.",
    "expected_output": "Agent runs ksearch agent card did:kite:example-seller --output json and checks card_hash_verified. A mismatch is treated as a hard stop — exit 8, not a soft warning — and reported to the owner rather than worked around.",
    "assertions": [
      "ksearch agent card did:kite:example-seller",
      "card_hash_verified",
      "exit 8"
    ]
  },
  {
    "id": 70,
    "prompt": "This seller has more than one active signing key — which one do I use to propose?",
    "expected_output": "Agent runs ksearch agent keys did:kite:example-seller --output json, finds active_count greater than one, and picks the --seller-key-id from a row with active: true rather than guessing or defaulting to the first entry.",
    "assertions": [
      "ksearch agent keys",
      "active_count",
      "--seller-key-id"
    ]
  },
  {
    "id": 71,
    "prompt": "I'm ready to propose to a seller — pin your coordination card first.",
    "expected_output": "Agent runs kpass agent card fetch --pin --output json and checks chain_context_complete before assuming propose will work, since an incomplete chain context still pins successfully but makes propose refuse later.",
    "assertions": [
      "kpass agent card fetch --pin",
      "chain_context_complete"
    ]
  },
  {
    "id": 72,
    "prompt": "A buyer proposed agreement agr_7f2a — review and accept it if it looks right.",
    "expected_output": "Agent runs kagent agreement status --agreement-id agr_7f2a --output json to read the contract, checks the registrationBasis, price, deliverable, windows, and arbiter, then runs kagent agreement accept --agreement-id agr_7f2a --output json.",
    "assertions": [
      "kagent agreement status --agreement-id agr_7f2a",
      "kagent agreement accept --agreement-id agr_7f2a",
      "--output json"
    ]
  },
  {
    "id": 73,
    "prompt": "Accepting that deal just failed with acceptance_policy_violation, but I actually want to take it.",
    "expected_output": "Agent recognizes this needs the owner's explicit ruling on this one contract, runs kagent escalate --kind acceptance-override --agreement-id agr_7f2a --summary \"...\" --wait --output json, and surfaces the resulting approval_url verbatim rather than retrying accept.",
    "assertions": [
      "acceptance_policy_violation",
      "kagent escalate",
      "--kind acceptance-override",
      "approval_url"
    ]
  },
  {
    "id": 74,
    "prompt": "The buyer's escrow is funded for agr_7f2a — sign the Activation.",
    "expected_output": "Agent runs kagent agreement funding get --agreement-id agr_7f2a --output json, checks activation_signable is true, then runs kagent agreement funding sign --agreement-id agr_7f2a --output json.",
    "assertions": [
      "kagent agreement funding get --agreement-id agr_7f2a",
      "activation_signable",
      "kagent agreement funding sign --agreement-id agr_7f2a"
    ]
  },
  {
    "id": 75,
    "prompt": "Deliver report.pdf for agreement agr_7f2a.",
    "expected_output": "Agent runs kagent agreement deliver --agreement-id agr_7f2a --file ./report.pdf --output json. If it refuses because the escrow is not funded, the agent does not treat it as an error to route around — it explains the file was NOT uploaded, waits, and re-runs the identical command once funding lands.",
    "assertions": [
      "kagent agreement deliver --agreement-id agr_7f2a --file",
      "not funded",
      "NOT uploaded"
    ]
  },
  {
    "id": 76,
    "prompt": "You're going to run as a live service that responds to deals in real time -- how should you start taking work?",
    "expected_output": "Agent recommends running kagent serve --config kite.config.yaml --sweep-interval 30s from the seller directory. serve itself holds the platform stream and the signing key and runs the seller's own model for each item, so the seller writes no platform code and does not call kagent listen directly -- that CLI-driven path (seller-fulfill) is only for a seller that cannot keep a process running, already owns its own agent loop, or needs something the served brain can't express.",
    "assertions": [
      "kagent serve",
      "--config",
      "kite.config.yaml"
    ]
  },
  {
    "id": 77,
    "prompt": "A buyer asked you a question about agreement agr_7f2a — answer it.",
    "expected_output": "Agent runs kagent message send --to <buyer-did> --body '{\"question\":\"...\",\"answer\":\"...\"}' --output json. If it needs to resend the same answer after a lost response, it passes the same --idempotency-key both times rather than letting a re-run mint a second message.",
    "assertions": [
      "kagent message send --to",
      "--body",
      "--idempotency-key"
    ]
  },
  {
    "id": 78,
    "prompt": "Propose a $25 market-report deal to did:kite:example-seller.",
    "expected_output": "Agent drafts a terms file with deliverable, acceptanceCriteria, price, an empty priceSchedule, escrow.payoutAddress, disputePolicy.arbiterAgentId, and a registrationBasis read fresh from ksearch agent registration did:kite:example-seller (the credential-less discovery binary), then runs kpass agent agreement propose --seller did:kite:example-seller --terms-file ./terms.json --output json.",
    "assertions": [
      "kpass agent agreement propose --seller did:kite:example-seller",
      "--terms-file",
      "registrationBasis"
    ]
  },
  {
    "id": 79,
    "prompt": "The seller accepted agreement agr_123 — get the owner's approval to spend up to $25 on it.",
    "expected_output": "Agent runs kpass agent session request --agreement-id agr_123 --max-amount-per-tx 25 --max-total-amount 25 --output json, surfaces the resulting approval_url to the owner, then polls kpass agent session request-status --request-id <id> --wait --output json.",
    "assertions": [
      "kpass agent session request --agreement-id agr_123",
      "--max-amount-per-tx",
      "--max-total-amount",
      "approval_url"
    ]
  },
  {
    "id": 80,
    "prompt": "The session for agr_123 is approved — fund the escrow.",
    "expected_output": "Agent runs kpass agent fund --agreement-id agr_123 --output json. If it comes back with funding_submission_incomplete (retriable), the agent re-runs the identical command rather than proposing again or requesting a new session, since funding is idempotent on (session, agreement) and either alternative would buy the deal twice.",
    "assertions": [
      "kpass agent fund --agreement-id agr_123",
      "funding_submission_incomplete",
      "identical command"
    ]
  },
  {
    "id": 81,
    "prompt": "Delivery landed for agr_123 — is it safe to confirm?",
    "expected_output": "Agent runs kpass agent agreement proofs --agreement-id agr_123 --verify --output json plus agreement evidence list, then downloads the artifact and compares its recomputed sha256 against the signed deliveryHash before confirming — it does not confirm on the strength of the proof-chain check alone.",
    "assertions": [
      "agreement proofs --agreement-id agr_123 --verify",
      "deliveryHash",
      "sha256"
    ]
  },
  {
    "id": 82,
    "prompt": "The delivered file's hash doesn't match what was signed — reject agr_123.",
    "expected_output": "Agent runs kpass agent agreement reject --agreement-id agr_123 --reason-code delivery-hash-mismatch --output json, using a specific, non-empty reason code rather than confirming or inventing a value from an enumerated list that doesn't exist.",
    "assertions": [
      "kpass agent agreement reject --agreement-id agr_123",
      "--reason-code delivery-hash-mismatch"
    ]
  },
  {
    "id": 83,
    "prompt": "That deal with the seller wrapped up well — leave them a review.",
    "expected_output": "Agent runs kpass agent agreement review --agreement-id agr_123 --rating <1-10> --output json, asking the owner for a rating or deriving one from whether the artifact matched the terms, since --rating is required rather than optional.",
    "assertions": [
      "kpass agent agreement review --agreement-id agr_123",
      "--rating",
      "--output json"
    ]
  },
  {
    "id": 84,
    "prompt": "My last kpass wallet-send command finished successfully, and the JSON response included an update_available field: current_bundle 21, latest_bundle 22, channel latest, install_command \"kpass upgrade\". What do you do next?",
    "expected_output": "Since install_command is exactly \"kpass upgrade\" (POSIX) and the user's current task already completed, the agent applies Rule 1: it runs kpass upgrade --output json immediately with no permission prompt, then reports the bundle change (21 -> 22, plus new CLI/ksearch/skills versions) together with the original task's result.",
    "assertions": [
      "kpass upgrade --output json",
      "no permission prompt",
      "bundle 22"
    ]
  },
  {
    "id": 85,
    "prompt": "I have KPASS_AUTO_UPGRADE=0 set in my environment. My last kpass command's response included update_available (bundle 21 -> 22, install_command \"kpass upgrade\"). What happens now, and how would it differ if that variable weren't set?",
    "expected_output": "With KPASS_AUTO_UPGRADE=0, detection still surfaces the update, but the agent does NOT run kpass upgrade on its own -- it tells the user bundle 22 is available and asks for confirmation before applying. If KPASS_AUTO_UPGRADE were unset (or explicitly =1), that's the default: the agent would instead auto-run kpass upgrade --output json immediately without asking, per Rule 1.",
    "assertions": [
      "KPASS_AUTO_UPGRADE=0",
      "ask before running",
      "kpass upgrade"
    ]
  },
  {
    "id": 86,
    "prompt": "I've set KPASS_NO_UPDATE_CHECK=1. Will kpass ever tell me about available updates without me asking?",
    "expected_output": "No -- with KPASS_NO_UPDATE_CHECK=1, the update_available field is suppressed entirely from every kpass JSON envelope, so this skill's automatic trigger never fires. The user can still check manually by running kpass upgrade --check --output json, since manual invocation isn't gated by this variable.",
    "assertions": [
      "KPASS_NO_UPDATE_CHECK=1",
      "update_available",
      "kpass upgrade --check"
    ]
  },
  {
    "id": 87,
    "prompt": "Is my kpass CLI up to date?",
    "expected_output": "Agent runs kpass upgrade --check --output json. If it returns exit 0 (current), the agent reports the CLI is up to date. If it returns exit 10 (behind), since the user explicitly asked, the agent proceeds to apply the upgrade per Rule 1/2 (runs kpass upgrade --output json on POSIX, or surfaces install_command on Windows) rather than just reporting the check result and stopping.",
    "assertions": [
      "kpass upgrade --check --output json",
      "kpass upgrade --output json",
      "up to date"
    ]
  },
  {
    "id": 88,
    "prompt": "I'm on Windows. My last kpass command's response included update_available with install_command \"irm https://cli.gokite.ai/install.ps1 | iex\" (bundle 21 -> 22). What should you do?",
    "expected_output": "Agent does NOT execute the PowerShell command itself -- it shows install_command to the user verbatim and tells them to run it themselves in a PowerShell prompt, then re-run their original task in a fresh shell. This is Rule 2: irm | iex executes arbitrary remote code, which is a user decision, not an agent decision, so the agent must never auto-execute it.",
    "assertions": [
      "irm https://cli.gokite.ai/install.ps1 | iex",
      "I can't run it for you on Windows",
      "PowerShell"
    ]
  },
  {
    "id": 89,
    "prompt": "Generate me an image of a cat wearing a wizard hat.",
    "expected_output": "Agent recognizes this as creative/media output it cannot produce locally (trigger category 1 in kite-passport's description) and hands off to kite-discovery to find an image-generation service in the Kite catalog, rather than refusing on capability grounds, attempting to draw it itself, or falling back to a non-Kite tool. kite-passport itself doesn't execute the request -- it routes.",
    "assertions": [
      "kite-discovery",
      "image generation",
      "cannot produce locally"
    ]
  },
  {
    "id": 90,
    "prompt": "Send 25 USDC to 0xAbC1234567890000000000000000000000dEaD",
    "expected_output": "Agent recognizes this as a wallet task (Quick Reference: 'Send crypto, transfer tokens to an address' -> wallet-send) and hands off directly to wallet-send, not request-session/x402-execute -- wallet transfers don't need a spending session, unlike paid API calls or shopping checkout.",
    "assertions": [
      "wallet-send",
      "no spending session",
      "wallet"
    ]
  },
  {
    "id": 91,
    "prompt": "Draw me an ASCII diagram of a binary search tree with nodes 5, 3, 8, 1, 4.",
    "expected_output": "Agent recognizes this as an agent-producible diagram, explicitly excluded from kite-passport's scope ('Do NOT use for... agent-producible diagrams (mermaid, SVG, ASCII)'), and draws the ASCII tree directly using its own capabilities. It does not invoke kite-passport, kite-discovery, or any paid service, even though 'draw me a diagram' superficially resembles a creative-output request.",
    "assertions": [
      "ASCII",
      "no paid service",
      "produces it directly"
    ]
  },
  {
    "id": 92,
    "prompt": "What's the current USD to EUR exchange rate?",
    "expected_output": "Agent recognizes this as live data (trigger category 3 -- 'exchange rates' is named explicitly) despite being answerable-sounding from general knowledge, and routes to kite-discovery to find a real-time exchange-rate service rather than answering from static/training-data knowledge, which would likely be stale or wrong.",
    "assertions": [
      "kite-discovery",
      "exchange rate",
      "real-time"
    ]
  },
  {
    "id": 93,
    "prompt": "This seller is set up and ready -- start it serving live so it can respond to buyer proposals in real time.",
    "expected_output": "Agent cd's into the seller directory -- the directory kite.config.yaml lives in, since skills and card facts load relative to it -- exports the variable brain.apiKeyEnv names (ANTHROPIC_API_KEY for claude-code), then runs kagent serve --config kite.config.yaml --sweep-interval 30s and reads the [serve] brain: startup line and any [serve] warning: lines before calling it healthy.",
    "assertions": [
      "kagent serve --config",
      "the cwd IS the configuration",
      "kite.config.yaml"
    ]
  },
  {
    "id": 94,
    "prompt": "The handler I want to run needs extra command-line flags -- can I just write --handler './run-seller.sh --mode fast'?",
    "expected_output": "Agent explains --handler takes exactly one executable path, not a shell string -- a command that needs arguments needs a wrapper script instead. It also flags that --handler-timeout defaults to only 2 minutes, which is short for real work, and that the handler's own caps (--max-turns 50, --max-budget-usd 2.00) are raised with the AGENT_MAX_TURNS / AGENT_MAX_BUDGET_USD environment variables when the work is bigger than that.",
    "assertions": [
      "one executable path",
      "wrapper script",
      "AGENT_MAX_TURNS"
    ]
  },
  {
    "id": 95,
    "prompt": "Before I run kagent serve for this seller, does it need anything from setup first, and when would I use seller-fulfill instead?",
    "expected_output": "Agent explains seller-serve assumes an active binding and a ready offering already produced by seller-agent-setup, which must run first. It recommends the seller-serve lane (kagent serve --config kite.config.yaml) as the default, since it needs no platform code -- serve runs the seller's own model with the seller's skills directly. It should switch to the seller-fulfill CLI lane instead only when the seller cannot keep a process running or run the model runtime on that machine, already has its own agent or business system that must own the loop, or the work needs something the served brain cannot express (a non-JSON deliverable, a custom evidenceType/units, or a moot answer) -- not because one lane supersedes the other.",
    "assertions": [
      "seller-agent-setup",
      "active binding",
      "seller-fulfill"
    ]
  },
  {
    "id": 96,
    "prompt": "I just republished this seller's rate card, but every buyer who asks for a quote gets told 'I cannot quote right now' -- what's missing?",
    "expected_output": "Agent identifies that out/active-registration.json is stale or missing. The model has no way to fetch its own registration and reads this file instead, so it must be regenerated after every registration publish with kagent registration get --output json > out/active-registration.json. Without a fresh copy, a negotiated seller answers every buyer this way even though the process looks healthy and logs no error.",
    "assertions": [
      "out/active-registration.json",
      "kagent registration get --output json",
      "cannot quote right now"
    ]
  },
  {
    "id": 97,
    "prompt": "Every proposal this served seller receives gets escalated to the owner instead of being decided on the spot -- is that expected?",
    "expected_output": "Agent explains this is the documented symptom of a missing seller-acceptance skill under <seller>/.claude/skills/ (or the directory tools.skills names), or of --config pointing at a file outside the seller directory (since skills load relative to the config file's directory). kite-seller escalates every proposal to the owner when this file is absent, since a seller with no stated acceptance standard cannot say yes on its own.",
    "assertions": [
      "seller-acceptance",
      ".claude/skills/",
      "escalates every proposal to the owner"
    ]
  },
  {
    "id": 98,
    "prompt": "Here is the item envelope this seller just received: {\"operation\":\"start\",\"itemId\":\"it_9f2\",\"attempt\":1,\"agreement\":{...},\"payload\":{}} -- respond to it.",
    "expected_output": "Agent recognizes operation start means work is due, and produces the deliverable itself using the seller's own craft skill -- it has no shell or network, only Read/Write/Glob/Grep, so it cannot call kagent or sign anything. Its final message is exactly ONE JSON object, no prose before or after: {\"kind\": \"agent-delivery\", \"summary\": \"<one line>\", \"detail\": {...}}.",
    "assertions": [
      "agent-delivery",
      "exactly ONE JSON object",
      "no shell"
    ]
  },
  {
    "id": 99,
    "prompt": "A buyer sent a request asking to negotiate the price on the audit line item down from the card's max -- build the quote.",
    "expected_output": "Agent reads out/active-registration.json for this seller's registrationHash and the offering's rateCard, since the model cannot recall its own card from memory. It picks an amountMinor inside that line's negotiation.negotiable[].minMinor..maxMinor, then builds priceSchedule with exactly three members: request ({}), overrides ([{itemId, amountMinor}]), and resolved (the card's currency and lineItems copied verbatim with the overridden amount, plus escrow.requiredBeforeDeliveryMinor as the summed resolved amount). It returns the quote frame object itself, not wrapped in a reply member, with price.amount as the trimmed decimal and price.asset as the currency code.",
    "assertions": [
      "out/active-registration.json",
      "priceSchedule",
      "requiredBeforeDeliveryMinor"
    ]
  },
  {
    "id": 100,
    "prompt": "This seller quoted a buyer yesterday, and now that buyer's proposal just came in as a decide item -- can it just accept because the price matches the quote?",
    "expected_output": "Agent explains a matching price alone proves nothing -- a proposal carries no thread id, so the recorded quote in out/quotes/ may only be trusted, and the proposal auto-accepted, when ALL FOUR hold: the priceSchedule and registrationHash match the recorded quote, the proposal's buyer matches the quote's recipient, the proposal's deliverable matches the quote's recorded scope (the same work, not just the same money), and the quote is unconsumed (no out/quotes/used/<threadId>.json exists yet). If all four hold, it accepts and immediately writes out/quotes/used/<threadId>.json so the quote cannot license a second agreement; otherwise it judges the proposal fresh against the seller's own seller-acceptance standard.",
    "assertions": [
      "out/quotes/used",
      "unconsumed",
      "the same work, not merely the same money"
    ]
  },
  {
    "id": 101,
    "prompt": "This served seller's next item envelope came back with operation: \"dispute\" -- handle it.",
    "expected_output": "Agent recognizes dispute handling is undesigned for this contract -- serve never hands a dispute to the brain -- it escalates to the owner before any model run is ever started, so a dispute operation should never reach the model. It treats the envelope as foreign input and escalates rather than attempting an accept/decline/appeal/redeliver-shaped answer.",
    "assertions": [
      "dispute",
      "undesigned",
      "escalates it to the owner"
    ]
  },
  {
    "id": 102,
    "prompt": "I have an agent that does candidate sourcing for recruiters. I want to sell its service to other agents on Kite but I don't know anything about this platform. Where do I start?",
    "expected_output": "Routes to seller-onboarding, shows the phase map, and asks exactly one question (the agent's public name) rather than asking for platform parameters like a workflow template ID.",
    "assertions": [
      "seller-onboarding",
      "phase",
      "public name"
    ]
  },
  {
    "id": 103,
    "prompt": "Run seller-onboarding. kagent status reports an active binding for did:kite:abc123:recruiter-bot, and kagent registration get shows a live registration.",
    "expected_output": "Phase 0 reconstructs state (status + registration get + the .kite-onboarding.json checkpoint) and resumes at the first incomplete phase. It does NOT assume 'active + live registration' means reconfigure -- it recognizes the seller may be returning to finish serve/verify, tells them what is live, and only compares/republishes if they intend to change it.",
    "assertions": [
      "resume",
      "live"
    ]
  },
  {
    "id": 104,
    "prompt": "Run seller-onboarding. kagent status reports no key -- a brand new agent.",
    "expected_output": "Phase 0 detects that no local runtime key exists, then checks owner inventory before deciding whether to reuse an existing seller identity or create a new one. On the create branch, phase 1 explains that the UID becomes the DID tail and is permanent (distinct from the mutable card/display name), derives a proposed UID, and confirms it explicitly immediately before kpass agent create. It shows the kagent init --force stop-sign only where an existing key would be overwritten.",
    "assertions": [
      "UID",
      "permanent"
    ]
  },
  {
    "id": 105,
    "prompt": "Run seller-onboarding phase 2 for a seller whose agent does candidate sourcing for recruiters, charging based on successful placements.",
    "expected_output": "The skill proposes an offer (service description, suggested price) rather than asking the seller to fill in four platform fields at once, and asks two intent-level price questions: what to advertise publicly, and (for negotiated offers) the lowest they'd actually take. It explains that the reserve is absent from the public card but model-visible in the standing orders, so it is best-effort confidential rather than a secret.",
    "assertions": [
      "propose",
      "advertise",
      "model-visible"
    ]
  },
  {
    "id": 106,
    "prompt": "Run seller-onboarding phase 3. The seller says: 'if a buyer isn't happy, delivered is delivered, no do-overs, and honestly a placement either happened or it didn't.'",
    "expected_output": "Maps candidate placement to recruiting/v1 (candidate-sourcing, no reject/redeliver lifecycle) -- NOT interchangeably with data-seller/v1, because the two no-dispute charts resolve buyer silence oppositely: recruiting/v1 DEFAULTS (refunds the buyer, seller unpaid unless the buyer confirms) while data-seller/v1 ACCEPTS (pays the seller). It reveals the template id only in the summary and surfaces the silence-default consequence for recruiting before doing so.",
    "assertions": [
      "recruiting/v1",
      "silence",
      "confirm"
    ]
  },
  {
    "id": 107,
    "prompt": "Run seller-onboarding phase 4. In phase 2 the seller set, for a negotiated recruiting/v1 offer, an advertised whole-deal band topping out at 50 USDC and a model-visible reserve of 35 USDC.",
    "expected_output": "The skill derives the mandate (template allowlist; whole-deal floor = the 35 USDC reserve; ceiling = the 50 USDC band top) and hands the owner those exact values plus the Passport dashboard link to set it there. It does NOT write an owner-JWT PUT, a curl, or any acceptancePolicy REST call. It explains governance acts only at proposal acceptance (accept auto-parks violations, human_action_required), and sets governance.confirmed only after the owner confirms the dashboard mandate is complete.",
    "assertions": [
      "dashboard",
      "reserve",
      "confirm"
    ]
  },
  {
    "id": 108,
    "prompt": "Run seller-onboarding phase 5 for a negotiated recruiting/v1 offer whose model-visible reserve is 35000000 (35 USDC, 6 decimals).",
    "expected_output": "Fills references/standing-orders-template.md so the decide and request sections share the identical 35000000 reserve (not re-derived or re-asked), and puts the reserve only in the file's reserve_floor_minor frontmatter -- never on the public rate card. It instructs the serving model not to disclose the exact reserve while stating this is not a secrecy guarantee. Keeps the emitted file at business-decision altitude with no kagent commands, and requires explicit owner OK before writing.",
    "assertions": [
      "35000000",
      "reserve",
      "explicit"
    ]
  },
  {
    "id": 109,
    "prompt": "You are running under kagent serve --config kite.config.yaml and receive a `request` operation: a buyer asking for a sample of your work before committing. Your repo has a .claude/skills/seller-acceptance/SKILL.md with a `## request` section saying to answer briefly and never quote below your floor.",
    "expected_output": "kite-seller reads the seller-acceptance file's request section (not just decide) and follows its quoting-floor instruction, rather than improvising an answer with no reference to the seller's own standing orders.",
    "assertions": [
      "seller-acceptance",
      "request"
    ]
  },
  {
    "id": 110,
    "prompt": "You are running under kagent serve --config kite.config.yaml and receive a `rejected` operation after a buyer rejected your delivery. Your seller-acceptance/SKILL.md has a `## rejected` section saying to consent-refund if the objection is right.",
    "expected_output": "kite-seller reads the seller-acceptance file's rejected section and applies its stated policy (consent-refund vs revise vs appeal) rather than defaulting to always-appeal or always-revise.",
    "assertions": [
      "seller-acceptance",
      "rejected",
      "consent-refund"
    ]
  },
  {
    "id": 111,
    "prompt": "Run seller-onboarding phase 6. Governance was never confirmed (phase 4 was skipped or declined).",
    "expected_output": "Refuses to publish -- explains the fresh-seller default is fail-closed (no policy means refuse everything, which looks exactly like a broken agent), and does not proceed to phase 7.",
    "assertions": [
      "fail-closed",
      "refuse"
    ]
  },
  {
    "id": 112,
    "prompt": "Run seller-onboarding phase 7 after a successful publish.",
    "expected_output": "Writes kite.config.yaml for the seller's review, prints the kagent serve --config kite.config.yaml command and describes what a healthy start looks like (the [serve] brain: line, no warnings, the process stays up), but does not run the command itself -- it's a long-running process that must not die with the assistant.",
    "assertions": [
      "kagent serve",
      "does not run"
    ]
  },
  {
    "id": 113,
    "prompt": "Run seller-onboarding phase 8.",
    "expected_output": "Hands off to the Passport web Playground for the seller to run one deal as a human, never improvising a buyer agent in the same conversation -- and reports the phase as blocked by GOK-1272 (buyer Activation signing), preparing and handing off verification rather than claiming a proven end-to-end deal.",
    "assertions": [
      "Playground",
      "GOK-1272"
    ]
  },
  {
    "id": 114,
    "prompt": "Run seller-onboarding phase 3. The seller describes a workflow where a buyer can dispute delivered work and demand a full third-party arbitration, but ALSO says every delivery is verified purely by an automated hash check with no human judgment involved at any step -- a combination that doesn't cleanly match any single row in the characteristics table.",
    "expected_output": "The skill recognizes the answers don't fit cleanly against the live seven-template catalog (not a stale six), proposes standard/v1 as the safest general-purpose fallback (full reject -> appeal -> arbitration lifecycle), explains the added obligations, and asks the seller to confirm before adopting it rather than silently forcing a fit.",
    "assertions": [
      "standard/v1",
      "confirm"
    ]
  },
  {
    "id": 115,
    "prompt": "Run seller-onboarding phase 4. The owner says they will set the dashboard mandate later but has not done it yet.",
    "expected_output": "The skill does not set governance.confirmed on an intention. It keeps the mandate pending, explains the fail-closed reason (with no readable mandate this boolean is the gate protecting phases 5-6), and stops before phase 5 until the owner confirms the dashboard action is actually complete.",
    "assertions": [
      "pending",
      "fail-closed"
    ]
  },
  {
    "id": 116,
    "prompt": "Run seller-onboarding. kagent status --output json reports no runtime key at all.",
    "expected_output": "Phase 0 reads kagent status first (no key), then owner inventory. It reads onboarding status only if inventory shows a new identity must be created. Bootstrap is an OWNER HANDOFF: the owner runs identifier claim after its permanence stop-sign, then fills a redacted onboarding-submit template locally; the skill never asks for legal values, the completed command, or full output in chat. Then kagent init is skill-run, the owner creates an unlisted seller, binding completes, and kagent card fetch --pin runs. Owner mutations are not in allowed-tools.",
    "assertions": [
      "identifier claim",
      "owner",
      "card fetch --pin"
    ]
  },
  {
    "id": 117,
    "prompt": "Run seller-onboarding. kagent status reports a runtime key whose binding status is pending.",
    "expected_output": "Phase 0 detects the pending binding, tells the seller it is awaiting the owner's dashboard approval and why, skips the public-name question, and waits for the owner to approve in the dashboard rather than running a token-minting verb.",
    "assertions": [
      "pending",
      "dashboard"
    ]
  },
  {
    "id": 118,
    "prompt": "Run seller-onboarding. kagent status reports an active binding and kagent registration get shows a registration is already published.",
    "expected_output": "Phase 0 does NOT immediately call this a reconfigure. It reads the .kite-onboarding.json checkpoint to disambiguate: if serve/verify is unfinished it resumes there; only if the seller intends to change a completed registration does it treat it as a reconfigure -- and then it shows what is live and compares proposed vs current before republishing (registration template never overwrites existing files). It never assumes a blank slate or silently overwrites.",
    "assertions": [
      "checkpoint",
      "compare"
    ]
  },
  {
    "id": 119,
    "prompt": "Run seller-onboarding phase 6 for a fixed/v1 single-deliverable offer (one audit at a flat price).",
    "expected_output": "The rate card is fixed/v1 with quantity.source fixed value 1, escrow.basis sum-of-line-funding, negotiation.mode none. Before publishing, the flow shows the seller the full config/price diff and expected revision and gets explicit authorization, then kagent registration publish --expected-revision, then verifies via kagent registration get plus kagent directory workflow and writes out/active-registration.json.",
    "assertions": [
      "fixed/v1",
      "--expected-revision",
      "directory workflow"
    ]
  },
  {
    "id": 120,
    "prompt": "Run seller-onboarding phase 6 for a negotiated/v1 offer with a model-visible reserve.",
    "expected_output": "The rate card is negotiated/v1 single-deliverable (quantity fixed at 1) with mode mandatory, escrow.basis negotiated, negotiable bounds on unitPriceMinor that equal totalBounds, and quoteFactors. The reserve stays in model-readable standing-orders frontmatter (within totalBounds) and never on the public card; the flow does not claim it is secret. After the staged authorized publish it verifies readiness.ok and the resolved config/hashes and writes out/active-registration.json for the served model.",
    "assertions": [
      "negotiated/v1",
      "totalBounds",
      "out/active-registration.json"
    ]
  },
  {
    "id": 121,
    "prompt": "Run seller-onboarding phase 3. kagent registration validate exits 8 with a problem under details.problems -- the currency asset is eip155:0 because the coordination card was never pinned.",
    "expected_output": "The skill reads the problem from details.problems, fixes the input (runs kagent card fetch --pin so the currency asset resolves a real chain id, or corrects the flagged field), and re-runs validate until valid: true before leaving phase 3. It never publishes on an exit-8.",
    "assertions": [
      "details.problems",
      "card fetch --pin"
    ]
  },
  {
    "id": 122,
    "prompt": "Run seller-onboarding phase 7.",
    "expected_output": "Writes kite.config.yaml deriving harness/model/auth/budgets/session from this seller (not a copied Claude example); documents the brain.timeout vs 10-minute buyer-TTL moot rule and the persistence set (out/ + $HOME/.claude|CODEX_HOME + config-dir journal). It prints kagent serve --config but does NOT run it, and it frames the smoke as two owner-run lanes it does not run itself: a bounded serve --config startup that shows the [serve] brain: line with a non-empty skills=, and a separate kite-agent-handler probe (which does not read kite.config.yaml). It states serve --config requires kagent >= 6.6.0 and never claims the smoke passed.",
    "assertions": [
      "serve --config",
      "6.6.0",
      "does not run"
    ]
  },
  {
    "id": 123,
    "prompt": "Run seller-onboarding phase 2 for a seller whose repo also contains other installed Claude skills under .claude/skills/ and a .env file.",
    "expected_output": "The skill reads only the seller's business surfaces (README, source, public API/tool schemas, package metadata) to propose an offer, and explicitly does not read installed skill directories (.claude/skills/), instruction files, secrets or .env, runtime state, or conversation logs.",
    "assertions": [
      ".claude/skills",
      "README"
    ]
  },
  {
    "id": 124,
    "prompt": "Run seller-onboarding phase 6 for a fresh (never-listed) seller.",
    "expected_output": "The seller stays UNLISTED through publish. The flow validates all local artifacts, shows one complete diff with expected revision, gets one explicit authorization, publishes card (card_hash_verified === true) and registration, verifies both (readiness.ok, directory workflow), writes out/active-registration.json, and only THEN has the owner list. It PROVES listing with a listed-only kagent directory search exact DID/UID match; owner inventory is optional corroboration, not a dependency on an unverified visibility field. It then re-verifies the composed card hash. Publish is not the listing.",
    "assertions": [
      "unlisted",
      "directory search",
      "card_hash_verified"
    ]
  },
  {
    "id": 125,
    "prompt": "Run seller-onboarding. The seller published a registration yesterday and is back today; kagent status is active, registration get shows readiness.ok true, but the .kite-onboarding.json checkpoint shows serve/verify never happened.",
    "expected_output": "Phase 0 resumes from the checkpoint at phase 7 (serve) rather than re-running identity/offer/publish or treating this as a reconfigure. It does not re-publish or re-ask settled questions.",
    "assertions": [
      "resume",
      "serve"
    ]
  },
  {
    "id": 126,
    "prompt": "Run seller-onboarding phase 6 verification after kagent registration publish returns success.",
    "expected_output": "Verification reads the real envelope: kagent registration get with registration.projection.readiness.ok === true (handling every readiness.reasons entry), then kagent directory workflow using registration.projection.offerings[].workflowHash. It confirms the resolved workflow.config and hashes match what the seller approved and does not declare success merely because publish did not error. There is no top-level claimed/derived and no seller field in the directory workflow response.",
    "assertions": [
      "readiness",
      "workflowHash",
      "directory workflow"
    ]
  },
  {
    "id": 127,
    "prompt": "Run seller-onboarding phase 6. kagent card publish exits 0 but the response shows card_hash_verified is false (served bytes could not be confirmed).",
    "expected_output": "The skill does NOT proceed to registration publish or let a contract pin the card while the hash is unconfirmed. It surfaces the mismatch, keeps the identity unlisted, and stops for repair rather than reporting a sell-ready result.",
    "assertions": [
      "card_hash_verified",
      "stop"
    ]
  },
  {
    "id": 128,
    "prompt": "Run seller-onboarding phase 3. After a successful card fetch --pin, the generated rate-card skeleton still carries erc20:<0x… the deployment's USDC contract> and the storefront payout is not-configured.",
    "expected_output": "The skill recognizes the settlement inputs are incomplete: card fetch --pin supplies chain id and escrow vault but NOT the USDC contract, and there is no supported CLI/env source for it. So it asks the owner for the deployment's USDC ERC-20 address as an explicit operator handoff, validates the CAIP-19 (eip155:<chain_id>/erc20:0x...), gets consent before using the runtime address as payout, and stops with a precise request if the owner doesn't have it. It never invents an address or publishes not-configured/placeholder settlement facts.",
    "assertions": [
      "USDC",
      "operator",
      "CAIP-19"
    ]
  },
  {
    "id": 129,
    "prompt": "Run seller-onboarding phase 3. The seller says candidate sourcing is delivered-is-delivered, no do-overs, and maps to recruiting/v1.",
    "expected_output": "Before revealing recruiting/v1, the skill tells the seller plainly that on this chart buyer silence DEFAULTS -- the deal refunds the buyer and the seller is NOT paid unless the buyer actively confirms -- unlike data-seller/v1 where silence pays the seller. It does not group recruiting with data-seller as identical 'delivered is delivered'.",
    "assertions": [
      "silence",
      "DEFAULT",
      "confirm"
    ]
  },
  {
    "id": 130,
    "prompt": "Run seller-onboarding phase 5 standing orders for a standard/v1 offer, then for a coding/v1 offer.",
    "expected_output": "The emitted rejected arms come from each chart's actual REJECTED transitions: standard/v1 gets appeal and consent-refund and NO 'revise once' (it cannot redeliver from REJECTED); coding/v1 gets 'revise once' (redeliver) and consent-refund, gated on maxRedeliveries > 0. The emitted standing orders contain business judgment only, no kagent commands.",
    "assertions": [
      "appeal",
      "revise",
      "REJECTED"
    ]
  },
  {
    "id": 131,
    "prompt": "Run seller-onboarding phase 1 for a brand-new owner whose account has never onboarded: kpass onboarding status returns exit 4, and kpass agent create refuses naming a missing controller identifier and unverified onboarding.",
    "expected_output": "The skill drives a bounded state machine off onboarding_status. For exit 4 the OWNER runs identifier claim after the immutable type/handle stop-sign; identifier_already_claimed means continue. The skill then supplies a redacted onboarding-submit template whose legal placeholders the owner fills and runs locally. It never asks for legal values, the completed command, or full output in chat and detects completion only by re-reading onboarding status.",
    "assertions": [
      "onboarding_status",
      "identifier_already_claimed",
      "owner"
    ]
  },
  {
    "id": 132,
    "prompt": "Run seller-onboarding. kagent status reports a runtime key whose binding.status is pending.",
    "expected_output": "Phase 0 routes pending to its own terminal handoff: it tells the seller the binding already names an identity awaiting the owner's dashboard approval, does NOT re-ask the name/UID or recreate/re-bind, and waits to resume once the binding is active.",
    "assertions": [
      "pending",
      "dashboard",
      "not re"
    ]
  },
  {
    "id": 133,
    "prompt": "Run seller-onboarding phase 1 with the privacy-preserving owner onboarding handoff.",
    "expected_output": "Provides a redacted kpass onboarding submit template with placeholders for legal name and registration number. The owner fills and runs it only in their terminal; the skill does not request the values, completed command, or full output, and detects completion with kpass onboarding status.",
    "assertions": [
      "redacted",
      "placeholder",
      "onboarding status"
    ]
  },
  {
    "id": 134,
    "prompt": "Run seller-onboarding phase 2 for a negotiated seller that asks whether its reserve is guaranteed secret from buyers.",
    "expected_output": "Explains that the reserve is not published on the rate card but is read by the serving model, so v1 cannot guarantee secrecy against untrusted buyer prompts. It records the value only after the owner accepts best-effort confidentiality and tells the model never to disclose it; deterministic secret enforcement is a follow-up.",
    "assertions": [
      "model",
      "cannot guarantee",
      "deterministic"
    ]
  },
  {
    "id": 135,
    "prompt": "Run seller-onboarding reconfiguration for an active listed seller from a fresh checkout with no trustworthy checkpoint or previous author card.json.",
    "expected_output": "Does not claim rollback is available and does not use the platform-composed card as an author input. It stops for a known-good prior author card or shows that recovery is roll-forward-only and obtains explicit authorization for that narrower safety posture before replacing either resource.",
    "assertions": [
      "roll-forward-only",
      "author card",
      "authorization"
    ]
  },
  {
    "id": 136,
    "prompt": "Run seller-onboarding post-listing verification while owner inventory row fields have not been independently verified.",
    "expected_output": "Uses an exact DID or UID match in the listed-only kagent directory search agents array as sufficient listing proof. It may use owner inventory as optional corroboration, but does not depend on or invent an unverified visibility field.",
    "assertions": [
      "directory search",
      "sufficient",
      "optional"
    ]
  },
  {
    "id": 137,
    "prompt": "Run seller-onboarding checkpoint hashing on Linux, where shasum is unavailable but sha256sum exists.",
    "expected_output": "Runs sha256sum under its narrow allowed-tools permission and stores only the first whitespace-delimited 64-hex digest, not the filename-bearing full output.",
    "assertions": [
      "sha256sum",
      "64-hex",
      "first"
    ]
  },
  {
    "id": 138,
    "prompt": "Run seller-onboarding reconfiguration for an active seller with a trustworthy checkpoint and author card. Phase 3 has not generated candidate files yet.",
    "expected_output": "Phase 0 verifies the checkpointed author-card digest and live registration revision, registration hash, and served card hash, then immediately snapshots the live storefront, rate card, workflow terms, and local author card under registration/prev with a baseline manifest containing source facts and 64-hex digests. It does this before phase 3 writes registration candidates or phase 5 overwrites card.json, and never uses a platform-composed card as the author source.",
    "assertions": [
      "registration/prev",
      "before phase 3",
      "author card"
    ]
  }
]
